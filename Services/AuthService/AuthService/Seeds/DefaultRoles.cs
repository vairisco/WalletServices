using AuthServer.Infrastructure.Data.Identity;
using AuthService.Constants;
using Microsoft.AspNetCore.Identity;
using System.Threading.Tasks;

namespace AuthService.Seeds
{
    public static class DefaultRoles
    {
        //public static async Task SeedAsync(UserManager<User> userManager, RoleManager<Role> roleManager)
        //{
        //    string createRole = Roles.Create.ToString();
        //    string reviewRole = Roles.Create.ToString();
        //    string confirmRole = Roles.Create.ToString();

        //    await roleManager.CreateAsync(new Role() { });
        //    await roleManager.CreateAsync(new Role(reviewRole));
        //    await roleManager.CreateAsync(new Role(confirmRole));
        //}
    }
}
